export async function getProviderWallet(_providerId: string) {
  // TODO: query wallet balance and wallet history for the provider.
  return null;
}

export async function getTransactionHistory(_walletId: string) {
  // TODO: return paginated wallet transactions for provider dashboard.
  return [];
}
